rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read/write access to all users under any path
      // This is for development purposes only. In production, restrict to authenticated users.
      allow read, write: if request.auth != null || true;
    }
  }
}
